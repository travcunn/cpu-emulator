sudo: false

language: c
compiler:
    - gcc-5
script:
    unlink /usr/bin/gcc && ln -s /usr/bin/gcc-5 /usr/bin/gcc
    make

addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - gcc-5

cache:
  apt: true
